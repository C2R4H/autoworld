import 'package:autoworld/main.dart';
import 'package:flutter/material.dart';

class home_view extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text('AutoWorld'),
      ),
      body: Container(
        child: Column(
          children: [],
        ),
      ),
    );
  }
}
